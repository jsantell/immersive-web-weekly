<!DOCTYPE html>
<html>
  <head>
      <title>Immersive Web Weekly: Issue #{{ issue }}, {{date}}</title>
      <link rel="stylesheet" type="text/css" href="../../styles/issue.css" />
  </head>
  <body>
    <div id="content">
      <h1>Immersive Web Weekly</h1>
      <div class="info">Issue #{{issue}}, {{date}}</div>
      <p>Preview/subtitle content: {{preview}}</p>
      {{#each links}}
          <h2><a href="{{url}}">{{title}}</a></h2>
          {{{content}}}
          <span class="domain"><a href="{{url}}">{{domain}}</a></span>
          <span class="author"><a href="{{authorLink}}">{{author}}</a></span>
          {{#each tags}}
             <span class="tag tag-{{this}}">{{this}}</span>
          {{/each}}
      {{/each}}
    </div>
  </body>
</html>
